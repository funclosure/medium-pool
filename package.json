{
  "name": "medium-pool",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "node scripts/check-skills.mjs",
    "test": "npm run check && astro build",
    "deploy": "wrangler pages deploy dist --project-name medium-pool",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.10",
    "three": "^0.185.1"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "yaml": "^2.9.0"
  },
  "description": "Every medium your app could speak: live demos plus an Agent Skill for each lane, kind, and composition.",
  "packageManager": "bun@1.3.10"
}
